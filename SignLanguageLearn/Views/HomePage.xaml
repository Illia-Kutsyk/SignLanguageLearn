<Page x:Class="SignLanguageLearn.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="HomePage" Background="{DynamicResource PrimaryBackground}">

    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="ContentGrid" 
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.0" To="1.0" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <Grid x:Name="ContentGrid" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource m_Panel}" FontSize="36" FontWeight="Bold" 
                   Foreground="{DynamicResource MainText}" Margin="15,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="2">
            <Button Style="{StaticResource CardButtonStyle}" Click="BtnLessons_Click">
                <StackPanel>
                    <TextBlock Text="🎓" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource m_Lessons}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource CardButtonStyle}" Click="BtnDictionary_Click">
                <StackPanel>
                    <TextBlock Text="📖" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource m_Dictionary}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource CardButtonStyle}" Click="BtnAchievements_Click">
                <StackPanel>
                    <TextBlock Text="🏆" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource m_Achievements}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource CardButtonStyle}" Click="BtnTests_Click">
                <StackPanel>
                    <TextBlock Text="📝" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource m_Tests}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </UniformGrid>
    </Grid>
</Page>